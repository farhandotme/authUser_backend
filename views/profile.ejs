<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-900 text-white flex flex-col items-center p-8 min-h-screen">
    <div class="w-full max-w-4xl">
        <div class="flex justify-between items-center mb-6">
            <div class="flex items-center">
                <h1 class="text-2xl">Hello, <%= user.name %></h1>
            </div>
            <a href="/logout" class="py-2 px-4 bg-purple-600 hover:bg-purple-700 rounded-md shadow-sm text-white text-sm font-medium">Logout</a>
        </div>
        <div class="mb-6">
          <h2 class="text-xl mb-2">You can now create a post</h2>
          <form action="/post" method="post">
            <textarea class="w-full px-3 py-2 border border-gray-600 rounded-md shadow-sm bg-gray-700 focus:outline-none focus:ring-purple-500 focus:border-purple-500 resize-none" rows="4" placeholder="What's on your mind....." name="content"></textarea>
            <button type="submit" class="mt-4 py-2 px-4 bg-purple-600 hover:bg-purple-700 rounded-md shadow-sm text-white text-sm font-medium">Create Post</button>
          </form>
      </div>
      <div class="space-y-4">
        <% user.posts.forEach((post) => { %>
          <div class="bg-gray-800 p-4 rounded-lg shadow-md">
            <p class="text-sm text-gray-400">@<%= user.username %></p>
            <p class="mt-2"><%= post.content %></p>
            <a class="text-green-400" href="/edit/<%= post._id %>">Edit</a>
          </div>
        <% }) %>
      </div>
    </div>
</body>

</html>
